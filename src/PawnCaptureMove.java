public class PawnCaptureMove extends CaptureMove {

    PawnCaptureMove(Board board, Piece movingPiece, int movingTo, Piece threatenedPiece) {
        super(board, movingPiece, movingTo, threatenedPiece);
    }
}
